# Edge-function secrets for local `supabase functions serve`.
# Copy to supabase/.env.local (gitignored). Rotate any key that has ever been shared.

GEMINI_API_KEY=your-rotated-gemini-key
GEMINI_MODEL=gemini-2.5-flash
